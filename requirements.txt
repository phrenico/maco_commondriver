# Exact-pin lockfile for fully reproducible environments.
# Canonical dependency declarations live in pyproject.toml [project.dependencies].
# To create a fresh environment matching the original paper results, run:
#   pip install -r requirements.txt
matplotlib==3.3.4
numpy==1.20.1
pandas==1.2.4
scikit_learn==1.1.3
scipy==1.6.2
torch==1.11.0
torchvision==0.12.0
tqdm==4.63.1
