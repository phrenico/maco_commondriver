[project]
name = "maco-env"
version = "0.1.0"
description = "UV environment for MaCo, AniSOM, shared baselines, and combine steps"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "cdriver",
]

[tool.uv.sources]
cdriver = { workspace = true, editable = true }
