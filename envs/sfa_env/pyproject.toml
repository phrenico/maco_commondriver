[project]
name = "sfa-env"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "cdriver",
    "numpy>=2.0.2",
    "scikit-learn>=1.6.1",
    "scipy>=1.13.1",
    "sklearn-sfa>=0.1.6",
    "tqdm>=4.67.3",
]

[tool.uv.sources]
cdriver = { workspace = true, editable = true }
